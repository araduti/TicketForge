apiVersion: v1
kind: Service
metadata:
  name: ticketforge
  namespace: ticketforge
  labels:
    app.kubernetes.io/name: ticketforge
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: ticketforge
