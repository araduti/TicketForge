# TicketForge dependencies
# Python 3.11+

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Data validation
pydantic==2.10.3
pydantic-settings==2.7.0

# HTTP client (async)
httpx==0.28.1

# Structured logging
structlog==24.4.0

# Embeddings (local, no GPU required)
sentence-transformers==3.3.1

# ML clustering
scikit-learn==1.6.0
numpy==2.2.0

# Database (SQLite ORM)
aiosqlite==0.20.0

# Database (PostgreSQL async driver)
asyncpg==0.30.0

# Rate limiting
slowapi==0.1.9

# Security / auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Prometheus metrics
prometheus-fastapi-instrumentator==7.0.0

# Utilities
tenacity==9.0.0
python-dotenv==1.0.1

# CORS middleware (built into FastAPI - no extra dep needed)

# Input sanitisation
nh3==0.2.21

# Database migrations
alembic==1.15.2
sqlalchemy==2.0.40

# OpenTelemetry distributed tracing
opentelemetry-api==1.33.0
opentelemetry-sdk==1.33.0
opentelemetry-instrumentation-fastapi==0.54b0
opentelemetry-exporter-otlp==1.33.0

# Error tracking (optional)
sentry-sdk[fastapi]==2.22.0

# GraphQL
strawberry-graphql[fastapi]==0.262.5

# Production server
gunicorn==23.0.0
