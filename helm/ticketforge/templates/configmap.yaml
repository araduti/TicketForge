apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ticketforge.fullname" . }}-config
  labels:
    {{- include "ticketforge.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
